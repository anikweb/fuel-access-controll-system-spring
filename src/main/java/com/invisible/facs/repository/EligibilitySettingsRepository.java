package com.invisible.facs.repository;

import com.invisible.facs.model.EligibilitySettings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EligibilitySettingsRepository extends JpaRepository<EligibilitySettings, Long> {
}
